---
import Base from '../layouts/Base.astro';

const inviteUrl =
  'https://discord.com/oauth2/authorize?client_id=1398118069771173978&permissions=294745402368&scope=bot%20applications.commands';

const features = [
  { label: 'View and browse games and characters', free: true, premium: true },
  { label: 'Join games and switch active context', free: true, premium: true },
  { label: 'Dice rolling', free: true, premium: true },
  { label: 'Create characters and stats', free: false, premium: true },
  { label: 'Roleplay proxying and message editing', free: false, premium: true },
  { label: 'Restore recently deleted characters', free: false, premium: true },
  { label: 'Inventory management', free: false, premium: true },
  { label: 'Game and campaign admin', free: false, premium: true },
  { label: 'Auto thread bumping', free: false, premium: true },
];

const includedLabel = 'Included';
const unavailableLabel = 'Premium only';
---

<Base title="Pricing" description="SPRITEbot server-wide premium access through Discord.">
  <section class="page-hero pricing-hero">
    <p class="eyebrow">Server-wide premium</p>
    <h1>Premium access for the whole table.</h1>
    <p>
      SPRITEbot premium unlocks character creation, campaign administration, inventory, thread
      automation, and roleplay proxying for an entire Discord server. One subscription covers every
      GM and player there.
    </p>
    <div class="notice-strip">
      <strong>Subscriptions live in Discord.</strong>
      <span>Run <code>/subscribe</code> in your server to see its status and current offer.</span>
    </div>
  </section>

  <section class="pricing-section" aria-labelledby="plans-heading">
    <div class="section-heading">
      <p class="eyebrow">Premium</p>
      <h2 id="plans-heading">Upgrade the server where your group already plays.</h2>
    </div>
    <div class="pricing-grid">
      <article class="price-card featured">
        <p class="card-kicker">Discord Premium App</p>
        <h3>SPRITEbot Premium</h3>
        <p>
          Use <code>/subscribe</code> in Discord to view or manage the subscription for the current
          server. Discord displays the current price before you subscribe.
        </p>
        <a class="button secondary" href={inviteUrl}>Add to Discord</a>
      </article>
    </div>
    <p class="pricing-note">
      Access is guild-scoped: an active Discord entitlement unlocks the Premium feature bundle for
      the server, while every server keeps the free core browsing and dice tools.
    </p>
  </section>

  <section class="comparison-section" aria-labelledby="comparison-heading">
    <div class="section-heading">
      <p class="eyebrow">Feature access</p>
      <h2 id="comparison-heading">Free players can browse and roll. Premium servers can create.</h2>
    </div>
    <div class="comparison-table-wrap">
      <table class="comparison-table">
        <thead>
          <tr>
            <th scope="col">Feature</th>
            <th scope="col">Free</th>
            <th scope="col">Premium</th>
          </tr>
        </thead>
        <tbody>
          {
            features.map((feature) => (
              <tr>
                <th scope="row">{feature.label}</th>
                <td>
                  <span class={feature.free ? 'status included' : 'status unavailable'}>
                    {feature.free ? includedLabel : unavailableLabel}
                  </span>
                </td>
                <td>
                  <span class={feature.premium ? 'status included' : 'status unavailable'}>
                    {feature.premium ? includedLabel : unavailableLabel}
                  </span>
                </td>
              </tr>
            ))
          }
        </tbody>
      </table>
    </div>
  </section>
</Base>
